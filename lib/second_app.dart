import 'package:flutter/material.dart';

void main(){
  // Flutter intro
  runApp(
      const Center (
          child: Text('This is my second Flutter application', textDirection: TextDirection.ltr,)
      )
  );
}